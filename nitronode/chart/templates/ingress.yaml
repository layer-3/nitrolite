{{- if and .Values.networking.ingress.enabled .Values.service.http.enabled }}
apiVersion: {{ include "nitronode.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "nitronode.common.fullname" . }}
  labels:
    {{- include "nitronode.common.labels" . | nindent 4 }}
  annotations:
    {{- include "nitronode.ingress.annotations" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.networking.externalHostname }}
      http:
        paths:
          {{- include "nitronode.ingress.httpPath" . | nindent 10 }}
  {{- include "nitronode.ingress.tls" . | nindent 2 }}
{{- end }}
